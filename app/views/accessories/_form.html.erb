<%= form_for(@accessory) do |f| %>
  <% if @accessory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@accessory.errors.count, "error") %> prohibited this accessory from being saved:</h2>

      <ul>
      <% @accessory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

<br>
    <%= f.hidden_field :not_missing, value: true %>

   <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>
  
  <%= f.hidden_field :bike_id, value: params[:bike_id] %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
